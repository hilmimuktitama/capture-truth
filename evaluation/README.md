# Synthetic contract evaluation

`npm run eval` runs deterministic synthetic contract cases covering clean input, review-safe portability, deprecated timestamps, missing provenance, candidate dedupe, portable raw exclusion, invalid timestamps, and duplicate source IDs. It reports case count, applicable seeded-defect recall, clean false positives, repeatability, and limitations. It makes no performance or quality claims.
